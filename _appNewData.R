# mobile UI to collect data
# last update: 2017-03-01

appNewData <- function(){
        tabPanel('Datenerfassung',
                 helpText('Erfasse hier einen neuen Eintrag im Template.')
        )
}